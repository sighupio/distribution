all:
  children:
    haproxy:
      hosts:
        haproxy:
          ansible_host: "1.2.3.4"
  vars:
    ansible_python_interpreter: python3
    ansible_ssh_private_key_file: "/cache/ci-ssh-key"
    ansible_user: "root"